/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OpenPipe } from './open.pipe';

describe('Pipe: Opene', () => {
  it('create an instance', () => {
    let pipe = new OpenPipe();
    expect(pipe).toBeTruthy();
  });
});
